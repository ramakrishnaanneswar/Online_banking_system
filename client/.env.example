# Frontend (client) environment variables
# Copy this file to `client/.env` for local development.
# For Vercel: leave unset so requests go to the same domain (/api).
# For Render: set to your Render backend URL, e.g. https://your-api.onrender.com/api
VITE_API_URL=http://localhost:3100/api
